services:
  datavis:
    build: .
    container_name: datavis-dashboard
    ports:
      - "3000:3000"
    volumes:
      # Mount all static files for Bun serving with write access for file watching
      - .:/app
    restart: unless-stopped
